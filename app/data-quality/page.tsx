import {redirect} from "next/navigation";import {getAccessContext} from "../access-control";import {getDataQualityReport} from "../../data-quality/data";import "./data-quality.css";export const dynamic="force-dynamic";const severity={critical:"高风险",warning:"需处理",info:"待完善"};export default async function DataQualityPage(){const user=await getAccessContext();if(!user)redirect("/");const report=await getDataQualityReport(user.organizationId);return <main className="quality-center"><header><div><small>DATA TRUST · CONTROL CENTER</small><h1>数据质量与系统可信度</h1><p>经营报表只能建立在完整的设备、耗材、采购和结算证据上。这里不修改业务数据，只指出缺口并引导处理。</p></div><nav><a href="/">返回驾驶舱</a><a href="/management-report">经营月报</a></nav></header><section className={`trust-score grade-${report.score>=75?"ok":"risk"}`}><div><span>当前可信度</span><strong>{report.score}</strong><em>/ 100 · {report.grade}</em></div><p>{report.method}</p><dl><div><dt>未闭环事项</dt><dd>{report.openIssues}</dd></div><div><dt>高风险类别</dt><dd>{report.critical}</dd></div><div><dt>检查时间</dt><dd>{new Date(report.generatedAt).toLocaleString("zh-CN")}</dd></div></dl></section><section className="quality-checks">{report.checks.map(row=><article key={row.key} className={row.count?row.severity:"clear"}><header><span>{severity[row.severity]}</span><em>{row.count?`扣 ${row.deduction} 分`:"已通过"}</em></header><strong>{row.count}</strong><h2>{row.label}</h2><p>{row.detail}</p><a href={row.href}>{row.count?"前往处理":"查看模块"} →</a></article>)}</section><section className="quality-boundary"><h2>可信度边界</h2><p>评分用于判断数据是否适合支持库存估值、成本核算和经营决策。它不会自动修正库存、猜测耗材绑定、批准采购成本或替代人工对账。</p></section></main>}
