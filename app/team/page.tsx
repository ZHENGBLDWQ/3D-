import {redirect} from "next/navigation";import {getAccessContext} from "../access-control";import TeamClient from "./team-client";export const dynamic="force-dynamic";export default async function TeamPage(){const user=await getAccessContext();if(!user)redirect("/");return <TeamClient user={user} signOut="/api/logout"/>}
