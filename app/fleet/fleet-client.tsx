"use client";
import {useEffect,useState} from "react";
type Slot={id:number;material:string;colorHex:string;remainingPercent?:number;amsUnit:number;trayIndex:number;active:boolean};
type Printer={id:number;name:string;model:string;status:string;connectionState:string;remoteProgress?:number;currentFile?:string;nozzleTemp?:number;bedTemp?:number;amsSlots?:Slot[]};
export default function FleetClient(){const[printers,setPrinters]=useState<Printer[]>([]),[hub,setHub]=useState("");useEffect(()=>{setHub(localStorage.getItem("layertraceHub")||"");const load=()=>fetch("/api/printers",{cache:"no-store"}).then(r=>r.json()).then(v=>setPrinters(v.printers||[])).catch(()=>{});void load();const timer=setInterval(load,10000);return()=>clearInterval(timer)},[]);const save=(value:string)=>{setHub(value);localStorage.setItem("layertraceHub",value)};return <main className="fleet-shell"><header className="fleet-top"><a href="/">← LayerTrace</a><div><small>LOCAL PRINT FARM · READ ONLY</small><h1>打印机监控墙</h1></div><label>本地摄像头网关<input value={hub} onChange={e=>save(e.target.value)} placeholder="http://192.168.1.10:1984"/></label></header><section className="fleet-grid">{printers.map(p=><article className="fleet-card" key={p.id}><div className="fleet-card-head"><div><small>{p.model||"BAMBU LAB"}</small><h2>{p.name}</h2></div><b>{p.status}</b></div><div className="fleet-progress"><strong>{Math.round(p.remoteProgress||0)}%</strong><i><span style={{width:`${p.remoteProgress||0}%`}}/></i><p>{p.currentFile||"当前没有打印任务"}</p></div><div className="ams-strip"><header><strong>AMS 耗材</strong><span>{p.amsSlots?.length||0} 槽</span></header><div>{p.amsSlots?.map(s=><section className={s.active?"active":""} key={s.id}><i style={{background:s.colorHex?`#${s.colorHex.slice(0,6)}`:"#dfe4e1"}}/><b>{s.material||"空"}</b><span>AMS{s.amsUnit}–{s.trayIndex+1}</span><em>{s.remainingPercent==null?"--":`${Math.round(s.remainingPercent)}%`}</em></section>)}</div></div><dl><div><dt>喷嘴 / 热床</dt><dd>{p.nozzleTemp?.toFixed(0)||"--"}° / {p.bedTemp?.toFixed(0)||"--"}°</dd></div><div><dt>连接状态</dt><dd>{p.connectionState}</dd></div></dl><div className="fleet-actions">{hub&&<a href={hub} target="_blank" rel="noreferrer">打开本地实时画面</a>}<a href="/monitor">查看打印会话与在机耗材</a></div></article>)}</section></main>}
