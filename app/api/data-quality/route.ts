import {getAccessContext} from "../../access-control";import {requireApiAccess} from "../../api-auth";import {getDataQualityReport} from "../../../data-quality/data";export async function GET(){const denied=await requireApiAccess();if(denied)return denied;const user=await getAccessContext();if(!user)return Response.json({error:"请先登录"},{status:401});return Response.json(await getDataQualityReport(user.organizationId))}
