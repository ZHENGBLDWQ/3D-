export type SupplierMetric={supplierId:number;code:string;name:string;orders:number;completedOrders:number;onTimeOrders:number;overdueOrders:number;avgLeadDays:number;approvedCents:number;actualCents:number;varianceCents:number;receivedSpools:number;qualityCases:number;activeOffers:number};
const clamp=(value:number,min=0,max=100)=>Math.min(max,Math.max(min,value));
export function scoreSupplier(row:SupplierMetric){const deliveryRate=row.completedOrders?row.onTimeOrders/row.completedOrders:0,completionRate=row.orders?row.completedOrders/row.orders:0,varianceRate=row.approvedCents?Math.abs(row.varianceCents)/row.approvedCents:0,qualityRate=row.receivedSpools?row.qualityCases/row.receivedSpools:0,deliveryScore=clamp(deliveryRate*100),completionScore=clamp(completionRate*100-row.overdueOrders*10),costScore=clamp(100-varianceRate*500),qualityScore=clamp(100-qualityRate*1000),score=Math.round(deliveryScore*.4+costScore*.25+qualityScore*.25+completionScore*.1),sampleConfidence=row.completedOrders>=10?"high":row.completedOrders>=3?"medium":"low";return {...row,deliveryRate,completionRate,varianceRate,qualityRate,deliveryScore,costScore,qualityScore,completionScore,score,sampleConfidence,grade:score>=90?"A":score>=75?"B":score>=60?"C":"D"}}
